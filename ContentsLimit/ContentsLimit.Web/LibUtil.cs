namespace ContentsLimit.Web
{
    /// <summary>
    /// Enum to be used when adding messages to TempData. Used in conjunction with bootstrap alert classes.
    /// </summary>
    public enum MessageType { Success, Error, Warning, Information }
}
